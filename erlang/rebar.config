{erl_opts, [debug_info]}.
{deps, [
    {cowboy, "2.12.0"},
    {jsx, "3.1.0"}
]}.

{project_app_dirs, ["apps/*", "sarc_gateway"]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]},
        {deps, []}
    ]}
]}.

{ct_opts, [
    {dir, ["sarc_gateway/test"]}
]}.
